<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SyndicApp.Mobile.Views.Residences.ResidencesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:layout="clr-namespace:SyndicApp.Mobile.Views.Layout"
    Title="Résidences"
    Shell.NavBarIsVisible="False"
    BackgroundColor="White">

    <layout:RoleDrawerLayout>
        <layout:RoleDrawerLayout.MainContent>

            <!-- ===== CONTENU SPÉCIFIQUE DE LA PAGE ===== -->
            <Grid BackgroundColor="White">
                <VerticalStackLayout Padding="16" Spacing="12">

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Résidences"
                               FontSize="22"
                               FontAttributes="Bold" />

                        <Button x:Name="BtnAddResidence"
                                Grid.Column="1"
                                Text="+"
                                BackgroundColor="#2563EB"
                                TextColor="White"
                                CornerRadius="12"
                                Padding="14,6"
                                Clicked="OnAddResidenceClicked" />
                    </Grid>

                    <CollectionView ItemsSource="{Binding Residences}"
                                    SelectionMode="None">
                        <CollectionView.EmptyView>
                            <Label Text="Aucune résidence"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </CollectionView.EmptyView>

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Margin="0,6"
                                       Padding="14"
                                       CornerRadius="12"
                                       HasShadow="False"
                                       BorderColor="#E5E7EB">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <VerticalStackLayout Spacing="2">
                                            <Label Text="{Binding Nom}"
                                                   FontAttributes="Bold"
                                                   FontSize="16" />
                                            <Label Text="{Binding Adresse}"
                                                   FontSize="12"
                                                   TextColor="#6B7280" />
                                            <Label Text="{Binding Ville}"
                                                   FontSize="12"
                                                   TextColor="#6B7280" />
                                        </VerticalStackLayout>

                                        <Label Grid.Column="1"
                                               Text="›"
                                               FontSize="22"
                                               VerticalOptions="Center"
                                               TextColor="#9CA3AF" />
                                    </Grid>

                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                                                              Path=BindingContext.OpenDetailsAsyncCommand}"
                                            CommandParameter="{Binding Id}" />
                                    </Frame.GestureRecognizers>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                </VerticalStackLayout>
            </Grid>

        </layout:RoleDrawerLayout.MainContent>
    </layout:RoleDrawerLayout>
</ContentPage>
