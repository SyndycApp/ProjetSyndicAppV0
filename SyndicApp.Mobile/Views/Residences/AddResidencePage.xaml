<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SyndicApp.Mobile.Views.Residences.AddResidencePage"
             Title="Nouvelle résidence">
	<ScrollView>
		<VerticalStackLayout Padding="24" Spacing="8">
			<Entry Placeholder="Nom" Text="{Binding Nom}" />
			<Entry Placeholder="Adresse" Text="{Binding Adresse}" />
			<Entry Placeholder="Ville" Text="{Binding Ville}" />
			<Entry Placeholder="Code postal" Text="{Binding CodePostal}" />
			<Button Text="Enregistrer" Command="{Binding SaveAsyncCommand}" />
			<ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>
