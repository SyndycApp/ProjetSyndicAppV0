<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:layout="clr-namespace:SyndicApp.Mobile.Views.Layout"
    x:Class="SyndicApp.Mobile.Views.Residences.ResidenceEditPage"
    Title="Modifier la résidence"
    Shell.NavBarIsVisible="False"
    BackgroundColor="White">

    <ContentPage.Resources>

        <Color x:Key="BodyText">#111827</Color>
        <Color x:Key="EntryBg">#F3F4F6</Color>
        <Color x:Key="EntryBorder">#E5E7EB</Color>

        <Style TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>

        <Style TargetType="Entry">
            <Setter Property="TextColor" Value="#111827"/>
            <Setter Property="PlaceholderColor" Value="#9CA3AF"/>
            <Setter Property="BackgroundColor" Value="Transparent"/>
            <Setter Property="HeightRequest" Value="44"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
        </Style>

    </ContentPage.Resources>


    <layout:RoleDrawerLayout>
        <layout:RoleDrawerLayout.MainContent>

            <ScrollView>
                <VerticalStackLayout Padding="16" Spacing="14">

                    <Label Text="Nom" FontAttributes="Bold"/>
                    <Border Stroke="{StaticResource EntryBorder}" StrokeThickness="1"
                           Background="{StaticResource EntryBg}" StrokeShape="RoundRectangle 8">
                        <Entry Text="{Binding Nom}" Placeholder="Résidence Al Amal"/>
                    </Border>

                    <Label Text="Adresse" FontAttributes="Bold"/>
                    <Border Stroke="{StaticResource EntryBorder}" StrokeThickness="1"
                           Background="{StaticResource EntryBg}" StrokeShape="RoundRectangle 8">
                        <Entry Text="{Binding Adresse}" Placeholder="Rue ..."/>
                    </Border>

                    <Label Text="Ville" FontAttributes="Bold"/>
                    <Border Stroke="{StaticResource EntryBorder}" StrokeThickness="1"
                           Background="{StaticResource EntryBg}" StrokeShape="RoundRectangle 8">
                        <Entry Text="{Binding Ville}" Placeholder="Casablanca"/>
                    </Border>

                    <Label Text="Code Postal" FontAttributes="Bold"/>
                    <Border Stroke="{StaticResource EntryBorder}" StrokeThickness="1"
                           Background="{StaticResource EntryBg}" StrokeShape="RoundRectangle 8">
                        <Entry Text="{Binding CodePostal}" Keyboard="Numeric" Placeholder="20000"/>
                    </Border>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="14" Margin="0,20,0,0">

                        <Button Text="Enregistrer"
                                BackgroundColor="#2563EB"
                                TextColor="White"
                                CornerRadius="12"
                                Command="{Binding SaveCommand}" />

                        <Button Grid.Column="1"
                                Text="Annuler"
                                BackgroundColor="#E5E7EB"
                                TextColor="#111827"
                                CornerRadius="12"
                                Clicked="Back_Clicked" />

                    </Grid>

                </VerticalStackLayout>
            </ScrollView>

        </layout:RoleDrawerLayout.MainContent>
    </layout:RoleDrawerLayout>

</ContentPage>
