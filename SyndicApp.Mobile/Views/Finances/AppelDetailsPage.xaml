<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SyndicApp.Mobile.Views.Finances.AppelDetailsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:conv="clr-namespace:SyndicApp.Mobile.Converters"
    Shell.NavBarIsVisible="False"
    BackgroundColor="White"
    Title="Appel de fonds">

    <ContentPage.Resources>
        <Color x:Key="TopbarColor">#2563EB</Color>
        <Color x:Key="HeaderTextOnDark">#FFFFFF</Color>
        <Color x:Key="BodyText">#111827</Color>
        <Color x:Key="Divider">#E5E7EB</Color>
        <Style TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="HorizontalOptions" Value="Start" />
        </Style>

        <conv:ProgressConverter x:Key="ProgressConverter" />
    </ContentPage.Resources>

    <Grid x:Name="Root" RowDefinitions="Auto,*">
        <!-- TOPBAR -->
        <Grid BackgroundColor="{StaticResource TopbarColor}" Padding="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BtnOpen"
                    Grid.Column="0"
                    Text="☰"
                    FontSize="22"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Clicked="OpenDrawer_Clicked"/>

            <Label Grid.Column="1"
                   Text="Appels De Fonds Détails"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"
                   FontAttributes="Bold"
                   TextColor="{StaticResource HeaderTextOnDark}"/>

            <Button Grid.Column="2"
                    Text="←"
                    FontSize="22"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Clicked="OnBackClicked"/>
        </Grid>

        <!-- CONTENU -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="16" Spacing="12">
                <Label Text="{Binding Appel.Description}" FontSize="24" FontAttributes="Bold"/>

                <Frame Padding="16" BorderColor="#E2E8F0" CornerRadius="14">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                        <Label Text="{Binding Appel.DateEmission, StringFormat='{0:dd/MM/yyyy}'}" />
                        <ProgressBar Grid.Row="1"
                                     Progress="{Binding Appel.MontantPaye, Converter={StaticResource ProgressConverter}, ConverterParameter={Binding Appel.MontantTotal}}"/>
                        <Grid Grid.Row="2" ColumnDefinitions="*,*,*"
                              Margin="0,10,0,0">
                            <VerticalStackLayout>
                                <Label Text="Montant total" FontSize="12"/>
                                <Label Text="{Binding Appel.MontantTotal, StringFormat='{0:N2} $'}" FontAttributes="Bold"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1">
                                <Label Text="Payé" FontSize="12"/>
                                <Label Text="{Binding Appel.MontantPaye, StringFormat='{0:N2} $'}" FontAttributes="Bold"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="2">
                                <Label Text="Restant" FontSize="12"/>
                                <Label Text="{Binding Appel.MontantReste, StringFormat='{0:N2} $'}" FontAttributes="Bold"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Grid>
                </Frame>

                <Label Text="Paiements" FontAttributes="Bold" />
                <CollectionView ItemsSource="{Binding Appel.Paiements}" SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnDefinitions="Auto,*,Auto" Padding="4,10">
                                <Label Text="$" FontSize="18" Margin="0,-2,8,0"/>
                                <VerticalStackLayout>
                                    <Label Text="{Binding Statut}" FontAttributes="Bold"/>
                                    <Label Text="{Binding DatePaiement, StringFormat='{0:dd/MM/yyyy}'}" FontSize="12"/>
                                </VerticalStackLayout>
                                <Label Grid.Column="2" Text="{Binding Montant, StringFormat='{0:N2} $'}" FontAttributes="Bold" VerticalOptions="Center"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Grid ColumnDefinitions="*,*,*" Margin="0,12,0,0">
                    <Button Text="Clôturer" Command="{Binding CloturerCommand}" />
                    <Button Grid.Column="1" Text="Modifier" Command="{Binding EditCommand}" />
                    <Button Grid.Column="2" Text="Supprimer" TextColor="Red" Command="{Binding DeleteCommand}" />
                </Grid>

                <Label Text="Description" FontAttributes="Bold" Margin="0,10,0,0"/>
                <Frame Padding="12" BorderColor="#E2E8F0" CornerRadius="10">
                    <Label Text="{Binding Appel.Description}" />
                </Frame>
            </VerticalStackLayout>
        </ScrollView>

        <!-- OVERLAY -->
        <BoxView x:Name="Backdrop"
                 Grid.RowSpan="2"
                 BackgroundColor="#66000000"
                 Opacity="0"
                 InputTransparent="True">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="Backdrop_Tapped"/>
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- DRAWER -->
        <Border x:Name="Drawer"
                Grid.RowSpan="2"
                BackgroundColor="White"
                StrokeThickness="0"
                TranslationX="-1000"
                Padding="0">
            <Grid RowDefinitions="Auto,*">
                <Grid BackgroundColor="{StaticResource TopbarColor}" Padding="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Text="Navigation"
                           TextColor="{StaticResource HeaderTextOnDark}"
                           FontAttributes="Bold"/>
                    <Button Grid.Column="1" Text="→" FontSize="22"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource HeaderTextOnDark}"
                            Clicked="CloseDrawer_Clicked"/>
                </Grid>

                <ScrollView Grid.Row="1">
                    <VerticalStackLayout Padding="16" Spacing="10">
                        <Label Text="Tableau de bord" FontAttributes="Bold"/>
                        <Button Text="Dashboard" Style="{StaticResource MenuButtonStyle}"
                                CommandParameter="//syndic-dashboard" Clicked="OnMenuItemClicked"/>
                        <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
                        <Label Text="Finances" FontAttributes="Bold"/>
                        <Button Text="Appels de fonds" Style="{StaticResource MenuButtonStyle}"
                                CommandParameter="//appels" Clicked="OnMenuItemClicked"/>
                        <Button Text="Créer un appel" Style="{StaticResource MenuButtonStyle}"
                                CommandParameter="appel-create" Clicked="OnMenuItemClicked"/>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Border>
    </Grid>
</ContentPage>
