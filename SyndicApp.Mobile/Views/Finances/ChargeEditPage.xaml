<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SyndicApp.Mobile.Views.Finances.ChargeEditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:SyndicApp.Mobile.ViewModels.Finances"
    x:DataType="viewmodels:ChargeEditViewModel"
    Title="Modifier charge"
    Shell.NavBarIsVisible="False"
    BackgroundColor="White">

    <ContentPage.Resources>
        <Color x:Key="TopbarColor">#2563EB</Color>
        <Color x:Key="HeaderTextOnDark">#FFFFFF</Color>
        <Color x:Key="BodyText">#111827</Color>
        <Color x:Key="Divider">#E5E7EB</Color>
        <Color x:Key="Chip">#F3F4F6</Color>

        <Style TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="HorizontalOptions" Value="Start" />
        </Style>

        <Style x:Key="LogoutButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource MenuButtonStyle}">
            <Setter Property="BackgroundColor" Value="{StaticResource Chip}" />
        </Style>

        <Style TargetType="Entry">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
            <Setter Property="BackgroundColor" Value="Transparent" />
        </Style>

        <Style TargetType="Editor">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
            <Setter Property="BackgroundColor" Value="Transparent" />
        </Style>

        <Style TargetType="Picker">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>
    </ContentPage.Resources>

    <Grid x:Name="Root"
          RowDefinitions="Auto,*"
          BackgroundColor="White">

        <!-- Topbar -->
        <Grid BackgroundColor="{StaticResource TopbarColor}"
              Padding="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="BtnOpen"
                    Grid.Column="0"
                    Text="☰"
                    FontSize="22"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Clicked="OpenDrawer_Clicked" />

            <Label Grid.Column="1"
                   Text="Modifier charge"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"
                   FontAttributes="Bold"
                   TextColor="{StaticResource HeaderTextOnDark}" />

            <Button Grid.Column="2"
                    Text="↩"
                    FontSize="22"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Clicked="OnBackClicked" />
        </Grid>

        <!-- FORM -->
        <ScrollView Grid.Row="1"
                    Padding="16">

            <VerticalStackLayout Spacing="14">

                <!-- Nom -->
                <Label Text="Nom de la charge" />
                <Border Stroke="Black"
                        StrokeThickness="1"
                        Margin="0,2,0,8"
                        Padding="4">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                    </Border.StrokeShape>
                    <Entry
                        Placeholder="Ex : Eau, Électricité..."
                        Text="{Binding Nom}" />
                </Border>

                <!-- Montant -->
                <Label Text="Montant (MAD)" />
                <Border Stroke="Black"
                        StrokeThickness="1"
                        Margin="0,2,0,8"
                        Padding="4">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                    </Border.StrokeShape>
                    <Entry
                        Keyboard="Numeric"
                        Placeholder="0,00"
                        Text="{Binding Montant}" />
                </Border>

                <!-- Date -->
                <Label Text="Date de la charge" />
                <Border Stroke="Black"
                        StrokeThickness="1"
                        Margin="0,2,0,8"
                        Padding="4">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                    </Border.StrokeShape>
                    <DatePicker
                        Date="{Binding DateCharge}" />
                </Border>

                <!-- Résidence -->
                <Label Text="Résidence" />
                <Border Stroke="Black"
                        StrokeThickness="1"
                        Margin="0,2,0,8"
                        Padding="4">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                    </Border.StrokeShape>
                    <Picker
                        Title="Choisir une résidence"
                        ItemsSource="{Binding Residences}"
                        SelectedItem="{Binding SelectedResidence}" />
                </Border>

                <!-- Bouton Enregistrer -->
                <Button Text="Enregistrer les modifications"
                        Margin="0,16,0,0"
                        CornerRadius="10"
                        Padding="14,10"
                        BackgroundColor="{StaticResource TopbarColor}"
                        TextColor="White"
                        FontAttributes="Bold"
                        Command="{Binding SaveCommand}" />
            </VerticalStackLayout>
        </ScrollView>

        <!-- BACKDROP -->
        <BoxView x:Name="Backdrop"
                 Grid.RowSpan="2"
                 BackgroundColor="#66000000"
                 Opacity="0"
                 InputTransparent="True">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="Backdrop_Tapped" />
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- DRAWER -->
        <Border x:Name="Drawer"
                Grid.RowSpan="2"
                BackgroundColor="White"
                StrokeThickness="0"
                TranslationX="-1000"
                Padding="0">
            <Grid RowDefinitions="Auto,*">

                <Grid BackgroundColor="{StaticResource TopbarColor}"
                      Padding="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Label Text="Navigation"
                           TextColor="{StaticResource HeaderTextOnDark}"
                           FontAttributes="Bold"
                           VerticalOptions="Center" />

                    <Button Grid.Column="1"
                            Text="←"
                            FontSize="22"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource HeaderTextOnDark}"
                            Clicked="CloseDrawer_Clicked" />
                </Grid>

 <ScrollView Grid.Row="1" BackgroundColor="White">
  <VerticalStackLayout Padding="16" Spacing="10">

    <!-- DASHBOARD -->
    <Label Text="Tableau de bord" FontAttributes="Bold"/>
    <Button Text="Dashboard"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//syndic-dashboard"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Dashboard Affectations"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//affectation-dashboard"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Mon espace (copro)"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//affectation-user-dashboard"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Gardien / Maintenance"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//affectation-maintenance-dashboard"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Statistiques affectations"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//affectation-analytics"
            Clicked="OnMenuItemClicked"/>

    <!-- RÉSIDENCES / LOTS -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Résidences &amp; Lots" FontAttributes="Bold"/>

    <Button Text="Résidences"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//residences"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Bâtiments"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//batiments"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Lots"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//lots"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Affectations des lots"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//affectation-lots"
            Clicked="OnMenuItemClicked"/>

    <!-- ❌ Occupant actuel & Historique affectations SUPPRIMÉS -->

    <!-- FINANCES -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Finances" FontAttributes="Bold"/>

    <Button Text="Appels de fonds"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//appels"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Charges"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//charges"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Paiements"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//paiements"
            Clicked="OnMenuItemClicked"/>

    <!-- INCIDENTS / INTERVENTIONS / DEVIS -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Incidents &amp; Interventions" FontAttributes="Bold"/>

    <Button Text="Incidents"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//incidents"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Interventions"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//interventions"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Devis de travaux"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//devis-travaux"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Prestataires"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//prestataires"
            Clicked="OnMenuItemClicked"/>

    <!-- ASSEMBLÉES GÉNÉRALES -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Assemblées Générales" FontAttributes="Bold"/>

    <Button Text="Convocations"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//ag/convocations"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Ordre du jour"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//ag/ordre-du-jour"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Votes"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//ag/votes"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Décisions"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//ag/decisions"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Procès-verbal (PV)"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//ag/pv"
            Clicked="OnMenuItemClicked"/>

    <!-- DOCUMENTS / ANNONCES -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Documents &amp; Annonces" FontAttributes="Bold"/>

    <Button Text="Documents"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//documents"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Catégories de documents"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//documents/categories"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Annonces"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//annonces"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Catégories d'annonces"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//annonces/categories"
            Clicked="OnMenuItemClicked"/>

    <!-- MESSAGERIE -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Messagerie" FontAttributes="Bold"/>

    <Button Text="Conversations"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//messages/conversations"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Messages"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//messages"
            Clicked="OnMenuItemClicked"/>

    <!-- PERSONNEL -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Personnel" FontAttributes="Bold"/>

    <Button Text="Employés"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//personnel/employes"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Plannings &amp; Présences"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//personnel/plannings"
            Clicked="OnMenuItemClicked"/>

    <!-- RECRUTEMENT -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Recrutement" FontAttributes="Bold"/>

    <Button Text="Offres d'emploi"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//recrutement/offres"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Candidatures"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//recrutement/candidatures"
            Clicked="OnMenuItemClicked"/>

    <!-- MARKETPLACE -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Marketplace" FontAttributes="Bold"/>

    <Button Text="Annonces de biens"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//marketplace/biens"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Demandes"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//marketplace/demandes"
            Clicked="OnMenuItemClicked"/>

    <!-- NOTIFICATIONS -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Notifications" FontAttributes="Bold"/>

    <Button Text="Centre de notifications"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//notifications"
            Clicked="OnMenuItemClicked"/>

    <!-- UTILISATEURS & RÔLES -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Utilisateurs &amp; Rôles" FontAttributes="Bold"/>

    <Button Text="Utilisateurs"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//admin/utilisateurs"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Rôles &amp; Permissions"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//admin/roles"
            Clicked="OnMenuItemClicked"/>

    <!-- PARAMÈTRES -->
    <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
    <Label Text="Paramètres" FontAttributes="Bold"/>

    <Button Text="Profil"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//settings/profil"
            Clicked="OnMenuItemClicked"/>

    <Button Text="Préférences"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//settings/preferences"
            Clicked="OnMenuItemClicked"/>

    <Button Text="À propos &amp; Support"
            Style="{StaticResource MenuButtonStyle}"
            CommandParameter="//settings/a-propos"
            Clicked="OnMenuItemClicked"/>

    <!-- DÉCONNEXION -->
    <BoxView HeightRequest="16" Opacity="0"/>
    <Button Text="Déconnexion"
            Style="{StaticResource LogoutButtonStyle}"
            CommandParameter="//login"
            Clicked="OnMenuItemClicked"/>
  </VerticalStackLayout>
</ScrollView>

            </Grid>
        </Border>
    </Grid>
</ContentPage>
