<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    x:Class="SyndicApp.Mobile.Views.Dashboard.SyndicDashboardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Dashboard"
    BackgroundColor="White"
    Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <Color x:Key="TopbarColor">#2563EB</Color>
        <Color x:Key="HeaderTextOnDark">#FFFFFF</Color>
        <Color x:Key="BodyText">#111827</Color>
        <Color x:Key="Divider">#E5E7EB</Color>
        <Color x:Key="Chip">#F3F4F6</Color>

        <Style TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="HorizontalOptions" Value="Start" />
        </Style>

        <Style x:Key="LogoutButtonStyle" TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
            <Setter Property="BackgroundColor" Value="{StaticResource Chip}" />
        </Style>

        <Style x:Key="KpiCardStyle" TargetType="Frame">
            <Setter Property="HasShadow" Value="False"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BackgroundColor" Value="White"/>
            <Setter Property="BorderColor" Value="#E5E7EB"/>
        </Style>

        <Style x:Key="KpiValueStyle" TargetType="Label">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="#111827"/>
        </Style>

        <Style x:Key="KpiLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextColor" Value="#6B7280"/>
        </Style>
    </ContentPage.Resources>

    <Grid x:Name="Root" RowDefinitions="Auto,*" BackgroundColor="White">

        <!-- TOPBAR -->
        <Grid BackgroundColor="{StaticResource TopbarColor}" Padding="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BtnOpen"
                    Grid.Column="0"
                    Text="☰"
                    FontSize="22"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Clicked="OpenDrawer_Clicked"/>

            <Label Grid.Column="1"
                   Text="Dashboard"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"
                   FontAttributes="Bold"
                   TextColor="{StaticResource HeaderTextOnDark}"/>

            <Label Grid.Column="2"
                   Text="• • •"
                   TextColor="{StaticResource HeaderTextOnDark}"
                   VerticalOptions="Center"/>
        </Grid>

        <!-- CONTENU -->
        <Grid Grid.Row="1" BackgroundColor="White">
            <ScrollView>
                <VerticalStackLayout Padding="20" Spacing="20">

                    <Label Text="Bienvenue" FontSize="22" FontAttributes="Bold" />

                    <!-- KPIs principaux -->
                    <Grid ColumnDefinitions="*,*"
                          RowDefinitions="Auto,Auto,Auto,Auto"
                          ColumnSpacing="12"
                          RowSpacing="12">

                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Row="0" Grid.Column="0">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding ResidencesCount}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Résidences" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Row="0" Grid.Column="1">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding LotsCount}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Lots" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Row="1" Grid.Column="0">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding BatimentsCount}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Bâtiments" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
                            <VerticalStackLayout Spacing="2">
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding IncidentsOuverts}" Style="{StaticResource KpiValueStyle}"/>
                                    <Label Text="↑" TextColor="#16A34A" VerticalTextAlignment="Center" />
                                </HorizontalStackLayout>
                                <Label Text="Incidents ouverts" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Row="3" Grid.Column="0">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding InterventionsEnCours}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Interventions en cours" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Row="3" Grid.Column="1">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding AppelsOuverts}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Appels de fonds (total)" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>
                    </Grid>

                    <!-- Finances -->
                    <VerticalStackLayout Spacing="12">
                        <Frame Style="{StaticResource KpiCardStyle}">
                            <VerticalStackLayout Spacing="8">
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="Taux de recouvrement" />
                                    <Label Grid.Column="1" Text="{Binding TauxRecouvrementPct}" FontAttributes="Bold"/>
                                </Grid>
                                <ProgressBar Progress="{Binding TauxRecouvrement}" HeightRequest="8"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}">
                            <VerticalStackLayout Spacing="8">
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="Résolution des incidents" />
                                    <Label Grid.Column="1" Text="{Binding TauxResolutionIncidentsPct}" FontAttributes="Bold"/>
                                </Grid>
                                <ProgressBar Progress="{Binding TauxResolutionIncidents}" HeightRequest="8"/>
                            </VerticalStackLayout>
                        </Frame>

                        <Frame Style="{StaticResource KpiCardStyle}">
                            <VerticalStackLayout Spacing="8">
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="Taux d’occupation des lots" />
                                    <Label Grid.Column="1" Text="{Binding TauxOccupationPct}" FontAttributes="Bold"/>
                                </Grid>
                                <ProgressBar Progress="{Binding TauxOccupation}" HeightRequest="8"/>
                            </VerticalStackLayout>
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Charges -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12" RowSpacing="12">
                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Column="0">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding ChargesCount}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Charges (nombre)" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>
                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Column="1">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding ChargesMontantTotal, StringFormat='{0:N0} MAD'}"
                                       Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Montant total des charges" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>
                    </Grid>

                    <!-- Docs / notifs -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12" RowSpacing="12">
                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Column="0">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding DocumentsCount}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Documents" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>
                        <Frame Style="{StaticResource KpiCardStyle}" Grid.Column="1">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding NotificationsNonLues}" Style="{StaticResource KpiValueStyle}"/>
                                <Label Text="Notifications non lues" Style="{StaticResource KpiLabelStyle}"/>
                            </VerticalStackLayout>
                        </Frame>
                    </Grid>

                    <BoxView HeightRequest="24" Opacity="0"/>
                </VerticalStackLayout>
            </ScrollView>
        </Grid>

        <!-- OVERLAY -->
        <BoxView x:Name="Backdrop"
                 Grid.RowSpan="2"
                 BackgroundColor="#66000000"
                 Opacity="0"
                 InputTransparent="True">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="Backdrop_Tapped"/>
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- DRAWER -->
        <Border x:Name="Drawer"
                Grid.RowSpan="2"
                BackgroundColor="White"
                StrokeThickness="0"
                TranslationX="-1000"
                Padding="0">
            <Grid RowDefinitions="Auto,*">
                <Grid BackgroundColor="{StaticResource TopbarColor}" Padding="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Text="Navigation"
                           TextColor="{StaticResource HeaderTextOnDark}"
                           FontAttributes="Bold"
                           VerticalOptions="Center"/>
                    <Button Grid.Column="1"
                            Text="×"
                            FontSize="22"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource HeaderTextOnDark}"
                            Clicked="CloseDrawer_Clicked"/>
                </Grid>

                <ScrollView Grid.Row="1" BackgroundColor="White">
                    <VerticalStackLayout Padding="16" Spacing="10">

                        <Label Text="Tableau de bord" FontAttributes="Bold"/>
                        <Button Text="Dashboard" Style="{StaticResource MenuButtonStyle}" CommandParameter="//syndic-dashboard" Clicked="OnMenuItemClicked"/>
                        <Button Text="Dashboard Affectations" Style="{StaticResource MenuButtonStyle}" CommandParameter="//affectation-dashboard" Clicked="OnMenuItemClicked"/>

                        <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
                        <Label Text="Résidences &amp; Lots" FontAttributes="Bold"/>
                        <Button Text="Résidences" Style="{StaticResource MenuButtonStyle}" CommandParameter="//residences" Clicked="OnMenuItemClicked"/>
                        <Button Text="Bâtiments" Style="{StaticResource MenuButtonStyle}" CommandParameter="//batiments" Clicked="OnMenuItemClicked"/>
                        <Button Text="Lots" Style="{StaticResource MenuButtonStyle}" CommandParameter="//lots" Clicked="OnMenuItemClicked"/>
                        <Button Text="Affectations des lots" Style="{StaticResource MenuButtonStyle}" CommandParameter="//affectation-lots" Clicked="OnMenuItemClicked"/>

                        <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" />
                        <Label Text="Finances" FontAttributes="Bold"/>
                        <Button Text="Appels de fonds" Style="{StaticResource MenuButtonStyle}" CommandParameter="//appels" Clicked="OnMenuItemClicked"/>
                        <Button Text="Charges" Style="{StaticResource MenuButtonStyle}" CommandParameter="//charges" Clicked="OnMenuItemClicked"/>

                        <BoxView HeightRequest="16" Opacity="0"/>
                        <Button Text="Déconnexion" Style="{StaticResource LogoutButtonStyle}" CommandParameter="//login" Clicked="OnMenuItemClicked"/>

                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Border>
    </Grid>
</ContentPage>
