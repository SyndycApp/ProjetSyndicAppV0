<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SyndicApp.Mobile.Views.Incidents.InterventionDetailsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:SyndicApp.Mobile.ViewModels.Incidents"
    x:DataType="vm:InterventionDetailsViewModel"
    x:Name="ThisPage"
    Shell.NavBarIsVisible="False"
    Title="Détails intervention"
    BackgroundColor="White">

    <!-- =========== RESSOURCES LOCALES =========== -->
    <ContentPage.Resources>
        <Color x:Key="TopbarColor">#2563EB</Color>
        <Color x:Key="HeaderTextOnDark">#FFFFFF</Color>
        <Color x:Key="BodyText">#111827</Color>
        <Color x:Key="Divider">#E5E7EB</Color>
        <Color x:Key="Chip">#F3F4F6</Color>

        <Style TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource BodyText}" />
        </Style>

        <Style x:Key="TitleLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontAttributes" Value="Bold" />
        </Style>

        <Style x:Key="CardStyle" TargetType="Frame">
            <Setter Property="HasShadow" Value="False" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="BackgroundColor" Value="White" />
            <Setter Property="BorderColor" Value="{StaticResource Divider}" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="#2563EB" />
            <Setter Property="TextColor" Value="White" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="12,8" />
        </Style>

        <Style x:Key="OutlineButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="TextColor" Value="#2563EB" />
            <Setter Property="BorderColor" Value="#2563EB" />
            <Setter Property="BorderWidth" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="10,8" />
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="#DC2626" />
            <Setter Property="TextColor" Value="White" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="10,8" />
        </Style>
    </ContentPage.Resources>

    <!-- =========== LAYOUT GLOBAL =========== -->
    <Grid RowDefinitions="Auto,*"
          BackgroundColor="White">

        <!-- ===== TOPBAR ===== -->
        <Grid BackgroundColor="{StaticResource TopbarColor}"
              Padding="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Bouton retour (ici lié à CancelCommand, comme dans ton VM) -->
            <Button Grid.Column="0"
                    Text="←"
                    FontSize="22"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Command="{Binding CancelCommand}" />

            <Label Grid.Column="1"
                   Text="Détails intervention"
                   HorizontalTextAlignment="Center"
                   VerticalOptions="Center"
                   FontAttributes="Bold"
                   TextColor="{StaticResource HeaderTextOnDark}" />

            <!-- Supprimer -->
            <Button Grid.Column="2"
                    Text="🗑"
                    FontSize="18"
                    BackgroundColor="Transparent"
                    TextColor="{StaticResource HeaderTextOnDark}"
                    Command="{Binding DeleteCommand}" />
        </Grid>

        <!-- ===== CONTENU ===== -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="16" Spacing="16">

                <!-- Loader -->
                <ActivityIndicator IsRunning="{Binding IsBusy}"
                                   IsVisible="{Binding IsBusy}"
                                   HorizontalOptions="Center" />

                <!-- DESCRIPTION & STATUT -->
                <Frame Style="{StaticResource CardStyle}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Description"
                               FontAttributes="Bold" />
                        <Label Text="{Binding Item.DescriptionSansGuid}"
                               LineBreakMode="WordWrap" />

                        <BoxView HeightRequest="1"
                                 BackgroundColor="{StaticResource Divider}"
                                 Margin="0,8" />

                        <Grid ColumnDefinitions="Auto,*"
                              VerticalOptions="Center">
                            <Label Text="Statut :"
                                   FontAttributes="Bold"
                                   VerticalOptions="Center" />

                            <!-- Badge arrondi pour le statut -->
                            <Frame Grid.Column="1"
                                   HasShadow="False"
                                   Padding="8,2"
                                   BackgroundColor="{StaticResource Chip}"
                                   CornerRadius="999"
                                   HorizontalOptions="End"
                                   VerticalOptions="Center">
                                <Label Text="{Binding Item.Statut}"
                                       HorizontalTextAlignment="Center"
                                       VerticalOptions="Center" />
                            </Frame>
                        </Grid>
                    </VerticalStackLayout>
                </Frame>

                <!-- DATES -->
                <Frame Style="{StaticResource CardStyle}">
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Dates"
                               FontAttributes="Bold" />

                        <Label Text="{Binding Item.DatePrevue, StringFormat='Prévue : {0:dd/MM/yyyy HH\\:mm}'}"
                               Opacity="0.7" />

                        <Label Text="{Binding Item.DateRealisation, StringFormat='Réalisation : {0:dd/MM/yyyy HH\\:mm}'}"
                               Opacity="0.7" />
                    </VerticalStackLayout>
                </Frame>

                <!-- COÛTS -->
                <Frame Style="{StaticResource CardStyle}">
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Coûts"
                               FontAttributes="Bold" />

                        <Label Text="{Binding Item.CoutEstime, StringFormat='Coût estimé : {0:F2}'}"
                               Opacity="0.7" />

                        <Label Text="{Binding Item.CoutReel, StringFormat='Coût réel : {0:F2}'}"
                               Opacity="0.7" />
                    </VerticalStackLayout>
                </Frame>

                <!-- ACTIONS SUR LE STATUT -->
                <Frame Style="{StaticResource CardStyle}">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Actions"
                               FontAttributes="Bold" />

                        <HorizontalStackLayout Spacing="10">

                            <Button Text="Démarrer"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Command="{Binding StartCommand}" />

                            <Button Text="Terminer"
                                    Style="{StaticResource OutlineButtonStyle}"
                                    Command="{Binding CompleteCommand}" />

                            <Button Text="Annuler"
                                    Style="{StaticResource DangerButtonStyle}"
                                    Command="{Binding CancelCommand}" />
                        </HorizontalStackLayout>

                        <Label Text="Les actions peuvent demander une date, un coût et un commentaire."
                               FontSize="12"
                               Opacity="0.6" />
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
